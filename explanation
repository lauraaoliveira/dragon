📚 EXPLICAÇÃO COMPLETA DO SISTEMA DE DRAGÕES
Vou explicar TODOS os conceitos de POO aplicados no código:

🏗️ 1. ESTRUTURA GERAL
src/
├── Main.java              // Interface com usuário
├── enums/                 // Valores fixos (constantes)
├── exceptions/            // Erros personalizados
├── interfaces/            // Contratos (obrigações)
├── abstracts/             // Classes base incompletas
└── models/                // Classes principais do sistema

🎯 2. CONCEITOS APLICADOS
📌 ENUM (Enumeração)
javapublic enum Elemento {
    FOGO, GELO, RAIO;  // Valores fixos
    
    public boolean temVantagemContra(Elemento outro) {
        return (this == FOGO && outro == GELO) ||
               (this == GELO && outro == RAIO) ||
               (this == RAIO && outro == FOGO);
    }
}
O que é: Tipo de dado com valores fixos e limitados.
Por que usar:

Evita erros de digitação ("FFOGO" não existe)
Garante que só existem 3 elementos possíveis
Pode ter métodos (como temVantagemContra)

Onde está:

Elemento.java - tipos de dragão
StatusDragao.java - SAUDAVEL, FERIDO, DERROTADO


⚠️ EXCEÇÕES PERSONALIZADAS
javapublic class EnergiaInsuficienteException extends Exception {
    public EnergiaInsuficienteException(String mensagem) {
        super(mensagem);
    }
}
O que é: Erros customizados para situações específicas.
Por que usar:

Trata problemas específicos do sistema
Dá mensagens mais claras
Permite capturar erros diferentes

Como funciona:
java// Lançar exceção:
if (energia < 15) {
    throw new EnergiaInsuficienteException("Sem energia!");
}

// Capturar exceção:
try {
    dragao.atacar();
} catch (EnergiaInsuficienteException e) {
    System.out.println(e.getMessage());
}
Onde está:

DragaoInvalidoException - dragão com dados inválidos
EnergiaInsuficienteException - energia insuficiente para atacar
TreinadorSemEspacoException - treinador com time cheio (6 dragões)


📜 INTERFACE
javapublic interface ICombate {
    int atacar() throws EnergiaInsuficienteException;
    void defender(int dano);
}
O que é: Um contrato que define quais métodos uma classe DEVE ter.
Por que usar:

Garante que toda criatura de combate pode atacar e defender
Permite polimorfismo (tratar diferentes classes igualmente)

Como funciona:
java// Dragao DEVE implementar TODOS os métodos da interface:
public class Dragao extends Criatura implements ICombate {
    @Override
    public int atacar() { ... }  // OBRIGATÓRIO
    
    @Override
    public void defender(int dano) { ... }  // OBRIGATÓRIO
}

🏛️ CLASSE ABSTRATA
javapublic abstract class Criatura {
    protected String nome;
    protected int vida;
    
    // Método ABSTRATO (sem implementação)
    public abstract void exibirInfo();
    
    // Método CONCRETO (com implementação)
    public void receberDano(int dano) {
        this.vida -= dano;
        // ... lógica completa
    }
}
O que é: Classe incompleta que serve de base para outras.
Diferença de Interface:

Pode ter métodos com e sem implementação
Pode ter atributos
Não pode ser instanciada (new Criatura() ❌)

Por que usar:

Compartilha código comum entre classes filhas
Define métodos que as filhas DEVEM implementar

Onde está:

Criatura.java - base para todos os seres vivos


👨‍👦 HERANÇA
javapublic class Dragao extends Criatura implements ICombate {
    // Dragao HERDA tudo de Criatura
    // + implementa ICombate
}

public class DragaoDeFogo extends Dragao {
    // DragaoDeFogo HERDA tudo de Dragao
    // que HERDA tudo de Criatura
}
O que é: Uma classe "filho" recebe tudo da classe "pai".
Hierarquia:
Criatura (abstrata)
    ↓
Dragao (abstrata) + ICombate
    ↓
DragaoDeFogo, DragaoDeGelo, DragaoDeRaio
O que é herdado:

Atributos: nome, vida, energia
Métodos: receberDano(), estaVivo()


🔒 ENCAPSULAMENTO
javapublic class Criatura {
    protected String nome;     // Visível para filhas
    private int vida;          // Só dentro desta classe
    
    public int getVida() {     // Acesso controlado
        return vida;
    }
}
O que é: Esconder dados internos e controlar o acesso.
Modificadores de acesso:

private - só dentro da classe
protected - classe + filhas
public - todo mundo

Por que usar:

Protege dados de alterações indevidas
Centraliza validações nos setters
Facilita manutenção


🔄 POLIMORFISMO DE MÉTODO (Sobrecarga)
java// Mesmo nome, parâmetros diferentes
public int atacar() throws EnergiaInsuficienteException {
    return poder;  // Ataque simples
}

public int atacar(Dragao oponente) throws EnergiaInsuficienteException {
    int dano = atacar();  // Chama o método acima
    if (this.elemento.temVantagemContra(oponente.getElemento())) {
        return (int)(dano * 1.5);  // Ataque com vantagem
    }
    return dano;
}
O que é: Múltiplos métodos com mesmo nome, mas parâmetros diferentes.
Java escolhe qual método usar baseado nos parâmetros:

atacar() → ataque normal
atacar(dragaoInimigo) → ataque com cálculo de vantagem


🎭 POLIMORFISMO DE CLASSE (Sobrescrita)
java// Na classe Dragao:
public abstract class Dragao extends Criatura {
    @Override  // Sobrescreve método da Criatura
    public void exibirInfo() {
        System.out.println(nome + " (" + elemento + ")");
    }
}

// Na classe DragaoDeFogo:
public class DragaoDeFogo extends Dragao {
    // Não sobrescreve, usa o método de Dragao
}
O que é: Classes filhas reimplementam métodos das classes pai.
Exemplo prático:
javaList<Dragao> dragoes = new ArrayList<>();
dragoes.add(new DragaoDeFogo("Inferno"));
dragoes.add(new DragaoDeGelo("Glacius"));

// Cada dragão usa SUA versão do método:
for (Dragao d : dragoes) {
    d.exibirInfo();  // Comportamento diferente para cada tipo
}

🏗️ CONSTRUTORES
javapublic class Dragao extends Criatura {
    
    // Construtor 1 (padrão)
    public Dragao(String nome, Elemento elemento) {
        super(nome, 100);  // Chama construtor da Criatura
        this.elemento = elemento;
        this.poder = 20;
    }
    
    // Construtor 2 (sobrecarregado)
    public Dragao(String nome, Elemento elemento, int poder) {
        super(nome, 100);
        this.elemento = elemento;
        this.poder = poder;  // Poder customizado
    }
}
O que é: Método especial para criar objetos.
Sobrecarga de construtores:

Mesmo nome da classe
Parâmetros diferentes
Java escolhe qual usar baseado nos argumentos

Uso:
javaDragao d1 = new Dragao("Smaug", Elemento.FOGO);           // Construtor 1
Dragao d2 = new Dragao("Alduin", Elemento.FOGO, 50);     // Construtor 2

🔗 RELACIONAMENTO DE CLASSES
1. Composição (HAS-A forte)
javapublic class Treinador {
    private List<Dragao> dragoes;  // Treinador TEM dragões
    
    public Treinador(String nome) {
        this.dragoes = new ArrayList<>();  // Cria a lista
    }
}
O que é: Uma classe possui outra e é responsável por ela.
Características:

Se o Treinador é destruído, os Dragões também são
Ciclo de vida dependente


2. Agregação (HAS-A fraco)
javapublic class Arena {
    public void iniciarBatalha(Dragao d1, Dragao d2) {
        // Arena USA dragões, mas não os possui
    }
}
O que é: Uma classe usa outra, mas não é dona.
Características:

Arena não cria nem destroi dragões
Dragões existem independente da Arena
Apenas recebe referências temporárias


3. Associação
javapublic int atacar(Dragao oponente) {
    // Dragao interage com outro Dragao
}
O que é: Classes que interagem entre si.

🎮 3. FLUXO DO PROGRAMA
Main.java - Interface com usuário
javapublic static void main(String[] args) {
    int opcao;
    
    do {  // DO-WHILE: executa pelo menos 1 vez
        exibirMenu();
        opcao = sc.nextInt();
        
        switch (opcao) {
            case 1 -> criarTreinador();
            case 2 -> criarDragao();
            case 3 -> listarTreinadores();
            case 4 -> batalhar();
        }
    } while (opcao != 0);  // Repete até usuário sair
}
Funções:

criarTreinador() - Cria novo treinador
criarDragao() - Cria dragão e adiciona a um treinador
listarTreinadores() - Mostra todos os treinadores e dragões
batalhar() - Inicia batalha entre 2 dragões


Exemplo de Fluxo Completo:
java// 1. Criar treinadores
Treinador ash = new Treinador("Ash");
Treinador misty = new Treinador("Misty");

// 2. Criar dragões
DragaoDeFogo charizard = new DragaoDeFogo("Charizard");
DragaoDeGelo articuno = new DragaoDeGelo("Articuno");

// 3. Adicionar dragões aos treinadores (Composição)
ash.adicionarDragao(charizard);
misty.adicionarDragao(articuno);

// 4. Batalha na arena (Agregação)
Arena arena = new Arena();
arena.iniciarBatalha(charizard, articuno);

// Durante a batalha:
try {
    int dano = charizard.atacar(articuno);  // Polimorfismo de método
    articuno.receberDano(dano);              // Método herdado de Criatura
} catch (EnergiaInsuficienteException e) {  // Exceção personalizada
    System.out.println(e.getMessage());
}


🤔 5. PERGUNTAS PARA FIXAR

Por que Criatura é abstrata?

Porque não faz sentido criar uma "Criatura genérica", só dragões específicos


Qual a diferença entre Interface e Classe Abstrata?

Interface: só assinaturas (contrato puro)
Abstrata: pode ter código implementado + atributos


Por que usar Enum ao invés de String?

Evita erros, garante valores válidos, permite métodos


O que acontece se não capturar a exceção?

Programa para/quebra com erro


Composição vs Agregação?

Composição: dona (Treinador possui Dragão)
Agregação: usa (Arena usa Dragão temporariamente)
